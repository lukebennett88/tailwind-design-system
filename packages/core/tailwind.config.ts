import { setmorePreset } from 'tailwind-config';
import { type Config } from 'tailwindcss';

// This config is only used to provide auto-complete
export default {
	content: [],
	presets: [setmorePreset],
} satisfies Config;
